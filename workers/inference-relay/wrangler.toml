name = "llmsafespace-inference-relay"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Deploy to all regions for maximum IP diversity
# Workers automatically run from the nearest POP to the caller
[placement]
mode = "smart"
