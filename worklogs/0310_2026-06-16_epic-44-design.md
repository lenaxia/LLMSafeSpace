# Worklog: Epic 44 — Session Reliability & Transparency design

**Date:** 2026-06-16
**Session:** Multi-session investigation, design, validation, and iteration for Epic 44
**Status:** Design complete, ready for implementation

---

## Objective

Design Epic 44 (Session Reliability & Transparency) in response to two production incidents:

1. **Incident A (workspace `a847faa5-19b4-463d-a434-1ce473a16f93`, session `ses_13076538bffeYtLrhoZ2ccRM1E`)**: Pod OOMKilled at 2026-06-16 08:47:44 UTC mid-stream. 16 sessions with ~1.6M total context tokens at OOM. Memory limit 2 GiB exceeded. Assistant turn was lost; session stuck busy in SQLite until next opencode restart.

2. **Incident B (workspace `8154ae86-d7b7-4f53-b046-d8d3b462b972`, session `ses_130c14344ffeVF52UQ6QGPmB0P`)**: User changed workspace secret bindings while a prompt was actively running. workspace-agentd SIGTERMed opencode at 16:04:14 UTC, destroying the in-flight assistant turn.

Both incidents were silent failures — users had no terminal feedback, no retry path, no error message.

---

## Work Completed

This entry covers four sessions of work:

### Session 1: Investigation (produced `INCIDENT-ANALYSIS.md`)

Reproduced both incidents from kubectl logs, opencode SQLite, and pod events:

- **Incident A root cause**: Pod cgroup OOM. Memory at OOM was 1.65 GiB (83%), 4.5 minutes before kill. Default memory limit comes from `controller/internal/workspace/pod_builder.go:249,251` (`defaultMemory = "512Mi"` × `burstFactor = 4` = 2 GiB). Workspace CRD supports `spec.resources.memory`/`memoryLimit` overrides which weren't set.

- **Incident B root cause**: workspace-agentd's secret reconciliation logic (`cmd/workspace-agentd/secrets.go:357,367,398`) restarts opencode unconditionally when env-secret or api-key bindings change. No check for busy sessions; no draining; no user notification.

- **Secret restart matrix**: Analyzed `cmd/workspace-agentd/secrets.go` to determine which secret types require restart vs. which could be hot-reloaded. env-secret and api-key write to env file (require restart since Node.js `process.env` is snapshot at startup); ssh-key, secret-file, git-credential, llm-provider all read filesystem at invocation (no restart needed).

- **Latent bug found**: `cmd/workspace-agentd/secrets.go:398` writes api-key changes to env file but does NOT trigger opencode restart. So api-key changes are silently ignored until next restart-for-other-reason. Aggressive deprecation recommended since no current users.

### Session 2: Validation (produced `DESIGN-VALIDATION.md`, `VALIDATION-COMPLETE.md`)

User explicitly requested all assumptions be validated, including "optional" ones. Validated 7 assumptions against source code:

| # | Assumption | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Exit code 137 detectable in supervisor | ✅ Confirmed | `cmd/workspace-agentd/main.go:1185` `cmd.Wait()` + `exitErr.ExitCode()` |
| 2 | cgroup v2 paths used correctly | ✅ Confirmed | `main.go:577-594` reads `/sys/fs/cgroup/memory.{current,max}` |
| 3 | sessionStatusTracker reconnects on disconnect | ✅ Confirmed | `connectAndRead` loop with exponential backoff `main.go:297-330` |
| 4 | Prometheus endpoint already exists | ✅ Confirmed | `/metrics` registered on admin port 4098 (`main.go:925`) |
| 5 | Frontend handles SSE error events | ⚠️ Partial | Generic `onerror` exists but no `event: error` type handling; needs new story |
| 6 | Marker file PVC-persists | ✅ Confirmed | `/home/workspace` backed by PVC (`main.go:140,433,436`) |
| 7 | "Ignore subsequent secret updates" pattern | ❌ User rejected | Multi-hour agentic flows make deferred restart very common; must handle all updates |

### Session 3: User Feedback Iteration (produced `QUESTIONS-ANSWERED.md`, `README-UPDATE-SUMMARY.md`, README-v1 → v2)

User clarified critical design points that required restructuring:

1. **Ops monitoring is P0** (was P2): "first-class citizen, not optional"
2. **Memory threshold = 85%** (was 75%)
3. **No timeout for deferred restart**: "agentic flows commonly run for multiple hours" → deferred restart will be VERY common, not rare edge case
4. **UI notification instead of forced restart**: "Pending refresh — click here to initiate (will halt sessions), or will auto-apply when sessions idle"
5. **Aggressive api-key deprecation**: "no api-key secrets currently in use"
6. **Request buffering NOT secret buffering**: My initial design buffered secret changes in agentd memory. User clarified the correct architecture:
   - Secrets written to DB IMMEDIATELY when user edits them
   - Mark workspace as "needs rematerialization"
   - When sessions idle: re-fetch ALL secrets from DB (idempotent)
   - NO in-memory secret buffer needed — DB is source of truth
   - Buffer the user's HTTP requests during restart instead (transparent to user)

This was a significant design correction. The wrong approach was confusing two different buffering concerns (secret state vs. request flow). The right approach treats the database as the source of truth for secret state and only buffers requests, which is what users actually experience.

### Session 4: Final Design (produced `DESIGN-FINAL.md`, current `README.md`)

Consolidated the 11 user stories across 3 phases:

**Phase 1 (P0): Stop Silent Failures** (~13 days)
- US-44.1: Terminal SSE events on agent death
- US-44.2: Session-aware restart (check busy sessions, defer until idle)
- US-44.3: Fix api-key restart bug (env changes silently ignored)
- US-44.4: OOM detection via exit code 137 + cgroup memory pressure
- US-44.5: Memory pressure warnings at 85% threshold
- US-44.10: API proxy request buffering during opencode restart

**Phase 2 (P1): User Experience** (~4 days)
- US-44.6: Frontend SSE error event handling
- US-44.7: UI notification for deferred restart (no forced timeout)

**Phase 3 (P2): Observability** (~6 days)
- US-44.8: Prometheus metrics — moved to P1 ("ops is first-class citizen")
- US-44.9: api-key deprecation with migration story
- US-44.11: Runbook + alerting rules

Total: ~23 days (Phase 1: 13d, Phase 2: 4d, Phase 3: 6d, per Epic 44 README). Originally estimated 14.5 days; expanded due to request buffering and UI work.

---

## Artifacts

All produced documents preserved in `design/stories/epic-44-session-reliability-transparency/`:

| File | Purpose |
|------|---------|
| `README.md` | Current canonical Epic 44 design (~666 lines) |
| `DESIGN-FINAL.md` | Consolidated final design summary |
| `INCIDENT-ANALYSIS.md` | Full investigation with kubectl evidence (546 lines) |
| `DESIGN-VALIDATION.md` | All 7 assumptions validated against source (614 lines) |
| `VALIDATION-COMPLETE.md` | Validation report summary |
| `QUESTIONS-ANSWERED.md` | User Q&A session record |
| `README-UPDATE-SUMMARY.md` | Change log for design iteration |
| `README-v1.md` | Original design (preserved for traceability) |
| `README-v2-SUMMARY.md` | Iteration change summary |
| `STUCK-SESSIONS-RECOVERY.md` | Operational guide for current stuck sessions |
| `SUMMARY.md` | Executive summary |

Root files (also part of Epic 44 work):
- `EPIC-44-ACTION-REQUIRED.md` — Status report at validation completion
- `VALIDATION-COMPLETE.md` — Duplicate at root for visibility

---

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| Ops monitoring is P0 (not P2 as initially designed) | User explicitly stated "first-class citizen, not optional" |
| Memory threshold = 85% (not 75%) | User-specified value based on operational experience |
| No timeout for deferred restart | Multi-hour agentic workflows make timeouts catastrophic |
| Deprecate api-key aggressively | No customers have api-key secrets defined; deprecation is safe |
| Request buffering, not secret buffering | Secrets go to DB immediately; rematerialization is idempotent; only HTTP requests need buffering |
| 11 user stories across 3 phases | Larger than initial 8-story estimate due to request buffering and UI work |
| Total estimate ~23 days | Per phase: P1=13d, P2=4d, P3=6d (Epic 44 README breakdown) |

---

## Blockers

None for the design phase. For implementation:
- US-44.1, US-44.4, US-44.5 (terminal events, OOM detection, memory pressure) — depends on agentd-side instrumentation; no external dependencies
- US-44.2, US-44.10 (session-aware restart, request buffering) — should land after Epic 45 US-45.2 (activeSess in Redis) for cleanest implementation
- US-44.6, US-44.7 (UI work) — depends on terminal event protocol from US-44.1

---

## Tests Run

None — design-only session. No code changes were made in this work; all changes were in `design/stories/epic-44-session-reliability-transparency/`.

Validation evidence (from DESIGN-VALIDATION.md) was collected by reading source code, not by running tests. Source references include:
- `cmd/workspace-agentd/main.go:577-594` (cgroup v2 paths)
- `cmd/workspace-agentd/main.go:1185` (exit code detection)
- `cmd/workspace-agentd/secrets.go:357,367,398` (secret restart logic)
- `controller/internal/workspace/pod_builder.go:249,251` (memory limit defaults)

---

## Next Steps

1. **Wait for PR #197 to merge** — provides the immediate mitigation
2. **Implement Epic 45 first** (in parallel with Epic 44 design):
   - US-45.1: stateStore abstraction
   - US-45.2: activeSess in Redis
   - This unblocks Epic 44 US-44.2 (session-aware restart) by giving cluster-wide visibility into busy sessions
3. **Begin Epic 44 Phase 1** (US-44.1, US-44.3, US-44.4, US-44.5, US-44.10): instrumentation work in agentd
4. **Update Epic 44 README** when implementation discovers any further design corrections

---

## Files Modified

Created in this multi-session work:

**Epic 44 design** (in `design/stories/epic-44-session-reliability-transparency/`):
- `README.md` — canonical design (~666 lines)
- `INCIDENT-ANALYSIS.md` — production incident investigation (546 lines)
- `DESIGN-VALIDATION.md` — assumption validation (614 lines)
- `STUCK-SESSIONS-RECOVERY.md` — operational runbook
- `history/README-v1.md`, `README-v2-SUMMARY.md`, `README-UPDATE-SUMMARY.md` — iteration artifacts
- `history/QUESTIONS-ANSWERED.md` — user Q&A record
- `history/SUMMARY.md`, `VALIDATION-COMPLETE.md`, `VALIDATION-COMPLETE-root.md`, `EPIC-44-ACTION-REQUIRED.md`, `DESIGN-FINAL.md` — session artifacts
- `history/README.md` — index of history folder

Modified:
- `design/stories/README.md` — added Epic 44 entry to index

---

## Lessons Learned

1. **Validate "optional" assumptions too**: I had marked some assumptions as "optional to validate" because they seemed likely correct. User correctly insisted on validating everything. Found 1 wrong assumption (frontend SSE error event handling) and 1 architectural confusion (request vs. secret buffering) that would have caused implementation rework.

2. **Distinguish state buffering from request buffering**: When a system needs to defer an operation, ask: are we buffering data, or buffering action? Confusing these leads to over-engineered solutions. The correct mental model: data should always go to the durable store immediately; actions can be deferred and replayed.

3. **Multi-hour workflows change everything**: Single-prompt LLM interactions and multi-hour agentic workflows have fundamentally different reliability requirements. A 30-second deferred restart is fine in the first model and catastrophic in the second.

4. **Production behavior tells you priorities**: "no api-key secrets currently in use" means we can deprecate aggressively. Always check empirical reality before designing for theoretical use cases.

---

## Related

- **PR #197**: Quick fix for stuck sessions (`reconcileSessionState`) shipped today as immediate mitigation while Epic 44 is in design
- **Worklog 0309**: PR #197's incident-fix worklog
- **Epic 45**: Multi-Replica State Consistency — addresses the underlying state-drift bug class that caused today's stuck sessions; will be designed and implemented in parallel with Epic 44
- **Epic 27a**: Credential Reload Foundation — provides the secret materialization plumbing that Epic 44 builds on
