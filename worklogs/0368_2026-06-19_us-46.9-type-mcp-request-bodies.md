# Worklog: Epic 46 Phase 2 — US-46.9 Type MCP Request Bodies

**Date:** 2026-06-19
**Session:** Replace map[string]any request bodies in pkg/mcp/client.go with typed structs
**Status:** Complete

---

## Objective

Replace 5 `map[string]interface{}` / `map[string]any` request body constructions in `pkg/mcp/client.go` with named structs with `json:` tags.

---

## Work Completed

Defined 5 typed structs and replaced all map literals:

| Struct | Replaces | Site |
|--------|----------|------|
| `questionEventResult` | SSE question-event result construction | line 301 |
| `questionReplyRequest` | POST /question/:id/reply body | line 358 |
| `permissionReplyRequest` | POST /permission/:id/reply body | lines 379-382 |
| `credentialProviderValue` | Credential "value" JSON payload | lines 390-396 |
| `createSecretRequest` | POST /api/v1/secrets body | lines 404-409 |

The `permissionReplyRequest.Message` field uses `json:"message,omitempty"` — same wire format as the old conditional `body["message"] = message` logic. The `credentialProviderValue.BaseURL` and `.Default` fields use `omitempty` — same as the old conditional inclusion.

---

## Verification

- `grep -n 'map[string]interface{}\|map[string]any' pkg/mcp/client.go` — **0 matches**
- `go build ./pkg/mcp/...` — passes
- `go test -timeout 60s -race ./pkg/mcp/...` — passes (2.2s)
- `gofmt -l` — clean

---

## Files Modified

| File | Action |
|------|--------|
| `pkg/mcp/client.go` | 5 map literals → typed structs; 5 new struct definitions |
| `design/stories/epic-46-codebase-debt-audit/US-46.9-type-mcp-request-bodies.md` | Status → Done |
| `worklogs/0368_2026-06-19_us-46.9-type-mcp-request-bodies.md` | Created |
