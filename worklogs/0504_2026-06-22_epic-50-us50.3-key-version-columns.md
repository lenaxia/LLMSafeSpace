# Worklog: Epic 50 — US-50.3 Key Version Columns

**Date:** 2026-06-22
**Session:** Add key_version columns + VersionedProvider interface + handler wiring.
**Status:** Complete.

## Work
- Migrations 42/43: `api_keys.key_version` + `org_sso_configs.key_version` (INTEGER NOT NULL DEFAULT 1).
- `VersionedProvider` interface + `ActiveVersionOf(p)` helper in root_key.go.
- Admin/org handler write paths use `secrets.ActiveVersionOf(h.provider)` instead of hardcoded 1.
- `APIKey.KeyVersion` DTO field added.
- Chart migration copies.

## Scope boundary
US-50.3 = columns + infrastructure + handler wiring. US-50.6 = every encrypt path (SSO, key_service) populates it.

## Tests
build OK; pkg/secrets + handler tests pass; lint 0 issues.
