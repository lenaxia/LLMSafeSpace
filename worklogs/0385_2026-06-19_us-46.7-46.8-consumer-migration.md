# Worklog: US-46.7 + US-46.8 Consumer Migration Gap Fix

**Date:** 2026-06-19
**Status:** Complete

Fixes the gaps identified in the post-merge review of PRs #258 and #263.

## US-46.8: All settings consumers now use Key constants
- Replaced ALL 20 remaining string-literal settings keys with settings.KeyXxx.Name() across workspace_service.go, max_active.go, auth.go, router.go, rate_limit.go
- Added 5 missing Key constants to registry (KeyRateLimitingStrategy, KeyRateLimitingWindowMinutes, KeyAuthRegistrationEnabled, KeyInstanceName, KeyInstanceMOTD)
- Result: grep for raw string-literal instance-settings keys returns 0

## US-46.7: Consumer migration to interface types
- SettingsReader: 2 consumers migrated (auth.go field+setter, rate_limit.go parameter)
- MeteringRecorder: 1 consumer migrated (middleware/metering.go field+constructor). 2nd candidate (ProxyHandler) blocked — it calls both Record and CheckQuota, so it can't be narrowed to the 2-method recorder interface without splitting into separate QuotaChecker interface.
- WorkspaceMetadataStore: 0 consumers migrated (deferred). Handlers use different method subsets; the ModelStore mini-interface in models_handler.go wraps GetDefaultModel which isn't on WorkspaceMetadataStore. Consolidation requires a larger refactor.
