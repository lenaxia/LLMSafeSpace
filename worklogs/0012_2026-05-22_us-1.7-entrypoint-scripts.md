# Worklog: US-1.7 Create Entrypoint Scripts

**Date:** 2026-05-22
**Session:** Implement entrypoint scripts for opencode serve in sandbox containers
**Status:** Complete

---

## Objective

Create `entrypoint-common.sh` and `entrypoint-opencode.sh` in `runtimes/base/tools/entrypoints/` so every sandbox container starts `opencode serve` with correct configuration, credentials, and environment variables.

---

## Work Completed

### 1. Assumption Validation

All assumptions validated from documentation before writing any code:

| Assumption | Source | Result |
|---|---|---|
| `entrypoints/` directory does not exist | `ls runtimes/base/tools/` | Confirmed — only cleanup-pod, execution-tracker, health-check, sandbox-monitor present |
| `OPENCODE_SERVER_PASSWORD` is env var (not CLI flag) | EVOLUTION-V2.md §7.1a | Confirmed — "OPENCODE_SERVER_PASSWORD — if set, enables Basic Auth on all endpoints" |
| `XDG_DATA_HOME=/workspace/.local` is correct override | EVOLUTION-V2.md §7.9, §7.7 | Confirmed — "To persist to PVC, set XDG_DATA_HOME=/workspace/.local" |
| `OPENCODE_CONFIG=/tmp/agent-config.json` is correct | US-1.7 story spec + §7.1a | Confirmed |
| `opencode serve --hostname 0.0.0.0 --port 4096` is correct | EVOLUTION-V2.md §7.1a | Confirmed — "--hostname and --port are CLI flags" |
| Credentials from `/sandbox-cfg/credentials` → `/tmp/agent-config.json` | US-1.7 story spec + §7.1 | Confirmed — "entrypoint-common.sh copies to /tmp/agent-config.json" |

### 2. Comment Evaluation (README-LLM.md Rule 4)

Rule 4: "No comments unless strictly necessary and timeless."

- `entrypoint-common.sh` story spec contains `# Materialize credentials from init container volume to config file` — this describes what the code (`cp /sandbox-cfg/credentials /tmp/agent-config.json`) already expresses. The reader can understand intent from the code alone. **Removed.**
- `entrypoint-opencode.sh` story spec has no comments. EVOLUTION-V2.md §7.7 has additional inline comments but those are documentation-level explanations, not code-level necessities. The code is self-documenting. **No comments added.**
- Added `set -euo pipefail` to `entrypoint-common.sh` as standard shell hardening (not in story spec but specified in the delegation task as a required addition).

### 3. Files Created

- `runtimes/base/tools/entrypoints/entrypoint-common.sh` — credential materialization
- `runtimes/base/tools/entrypoints/entrypoint-opencode.sh` — opencode serve runner

---

## Key Decisions

1. **Removed comment from entrypoint-common.sh**: The comment `# Materialize credentials from init container volume to config file` was in the story spec but violates README-LLM.md Rule 4. The `cp /sandbox-cfg/credentials /tmp/agent-config.json` line is self-documenting.

2. **Added `set -euo pipefail` to entrypoint-common.sh**: Story spec omits it but the delegation task explicitly requires it for production shell robustness. `entrypoint-opencode.sh` already has it in the story spec.

3. **Script content is otherwise verbatim from story spec**: No other deviations. All env vars, paths, and command invocations match the spec exactly.

---

## Blockers

None.

---

## Tests Run

Shell scripts are not Go — TDD does not directly apply. Validation performed:

```
bash -n entrypoint-common.sh    → syntax OK
bash -n entrypoint-opencode.sh  → syntax OK
shellcheck                      → not installed, skipped
```

File permissions confirmed: both files are `-rwxr-xr-x` (chmod 755).

---

## Next Steps

US-1.7 is complete. The next story in Epic 1 should be checked against the story list in `design/stories/README.md`. The Dockerfile at `runtimes/base/Dockerfile` references these scripts at `/usr/local/bin/entrypoint-common.sh` and `/usr/local/bin/entrypoint-opencode.sh` (COPY instructions in §13.2) — that Dockerfile integration is owned by the Dockerfile story, not this one.

---

## Files Modified

- `runtimes/base/tools/entrypoints/entrypoint-common.sh` (created)
- `runtimes/base/tools/entrypoints/entrypoint-opencode.sh` (created)
