# Worklog: Epic 30 US-30.4 — Replace Free-Tier Opencode Key

**Date:** 2026-06-05
**Session:** Remove hardcoded OPENCODE_AUTH_CONTENT, seed via API startup
**Status:** Complete

---

## Objective

Replace the hardcoded `OPENCODE_AUTH_CONTENT` env var in `pod_builder.go` with a real admin credential seeded at API startup. The free-tier key now flows through the same `FormatOpenCodeConfig` path as all other credentials.

---

## Work Completed

- Removed `OPENCODE_AUTH_CONTENT` env var injection from `pod_builder.go:73`
- Removed `buildOpenCodeAuthContent()` function (pod_builder.go:479-496)
- Removed `inference_relay_test.go` (tested the removed function)
- Removed unused `encoding/json` import from pod_builder.go
- Created `ensureFreeTierCredential()` in `secrets_adapters.go` — encrypts `{"provider":"opencode","apiKey":"public"}` with server KEK and calls `UpsertFreeTierCredential`
- Wired at API startup in app.go (non-fatal on failure — logs warning)

---

## Tests Run

```
go build ./...         → OK
go test -short -race   → 41 packages OK
make lint              → 0 issues
```

---

## Next Steps

US-30.5: Rewrite `PrepareSecretsForInjection` with multi-source query and priority merge.

---

## Files Modified

- `controller/internal/workspace/pod_builder.go` — removed OPENCODE_AUTH_CONTENT + buildOpenCodeAuthContent + json import
- `controller/internal/workspace/inference_relay_test.go` — deleted
- `api/internal/app/secrets_adapters.go` — added `ensureFreeTierCredential()`
- `api/internal/app/app.go` — wired startup call
