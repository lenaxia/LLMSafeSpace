# Worklog: Epic 24 CRD Cleanup — Remove Deprecated Fields

**Date:** 2026-06-05
**Session:** Remove TransientFailureCount, LastTransientFailureAt, MaxRetries, and related constants. File issue #38 for deferred items.
**Status:** Complete

---

## Objective

Remove deprecated CRD fields that were superseded by Epic 24's recovery system. These fields are no longer read or written by any code path.

---

## Work Completed

### Removed

| Field | Location | Replaced By |
|-------|----------|-------------|
| `TransientFailureCount` | WorkspaceStatus | `ConsecutiveFailures` |
| `LastTransientFailureAt` | WorkspaceStatus | `LastFailureAt` |
| `MaxRetries` | WorkspaceSpec | Per-class `RecoveryPolicy.SafeModeAfter` |
| `MaxTransientFailures` | constants.go | Per-class policies |
| `TransientFailureResetWindow` | constants.go | `stabilityResetWindow` (2 min) |

### Files Modified

- `pkg/apis/llmsafespace/v1/workspace_types.go` — removed 3 fields
- `pkg/apis/llmsafespace/v1/zz_generated.deepcopy.go` — removed stale deepcopy block
- `charts/llmsafespace/crds/workspace.yaml` — removed from schema
- `pkg/crds/workspace_crd.yaml` — removed from schema
- `controller/internal/workspace/constants.go` — removed 2 constants
- `controller/internal/workspace/recovery.go` — removed old field references from handleFailed
- `controller/internal/workspace/phase_suspend.go` — removed old field clear
- `controller/internal/workspace/controller_test.go` — removed references
- `controller/internal/workspace/deletion_timestamp_test.go` — removed references
- `pkg/apis/llmsafespace/v1/workspace_types_test.go` — removed references

### Issue Filed

GitHub issue #38: "Epic 24 deferred: Safe mode, API recovery UX, Prometheus metrics"
— Documents all remaining Epic 24 work with priority recommendations.

---

## Key Decisions

- Removed fields outright (no deprecation period) per A10: no production users, no backwards compatibility constraints.
- `handleFailed` kept functional for legacy workspaces but no longer references old fields.

---

## Tests Run

```
go test -timeout 120s ./controller/... ./pkg/... → all pass, 0 failures
```

---

## Next Steps

See issue #38 for deferred Epic 24 items (safe mode, API UX, metrics).
