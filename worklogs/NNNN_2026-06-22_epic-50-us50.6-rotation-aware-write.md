# Worklog: Epic 50 — US-50.6 Rotation-Aware Write Path

**Date:** 2026-06-22
**Session:** Ensure every encrypt writes ActiveVersion into key_version columns.
**Status:** Complete.

## Work
- `auth.go`: CreateAPIKey sets `apiKey.KeyVersion = secrets.ActiveVersionOf(s.rootKeyProvider)` after encrypting key_ciphertext.
- `database.go`: CreateAPIKey INSERT adds `key_version` column; ListAPIKeysWithDecrypt SELECT reads it back.
- Admin/org handler write paths already wired in US-50.3 (ActiveVersionOf).

## Note
`UserKeyRecord.KeyVersion: 1` at key_service.go:160 is the per-user DEK version (password rotation count), not the server KEK version — correctly left as-is.

## Tests
build OK; vet OK; secrets + handler tests pass; golangci-lint 0 issues.
