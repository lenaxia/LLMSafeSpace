# Worklog: Epic 50 — US-50.7 Domain Separation

**Date:** 2026-06-22
**Session:** Separate apiKeyProv from Redis DEK-cache key (purpose "master-kek").
**Status:** Complete.

## Work
- app.go: apiKeyProv now uses deriveServerKey("master-kek") (active v2) +
  deriveServerKey("dek-cache") (legacy v1, decrypt-only) via multi-key provider.
  New encrypts use "master-kek"; existing rows still decrypt via trial.
- SSO KeyProvider switched from rkp to apiKeyProv (same domain separation).
- Redis DEK cache unchanged (still uses dek-cache).
- Sealed provider path: when rkp is sealed, it wraps the raw root key — no
  purpose string applies, so rkp is used as-is (sealed is already domain-separated).

## Tests
build OK; app tests pass; sealed consumer test pass; lint 0 issues.
