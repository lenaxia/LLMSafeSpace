# Worklog: Story 6 — PortalLayout + Route Rename

**Date:** 2026-06-18
**Session:** Implement Story 6 from design 0031 (D2, D3) — extract PortalLayout from OrgAdminLayout, route already renamed.
**Status:** Complete — awaiting orchestrator review/merge

---

## Objective

Implement Story 6 of design `0031` (D2, D3):
1. **D2:** Extract a reusable `PortalLayout` component from `OrgAdminLayout`. Future portals (billing, platform admin) adopt the same primitive.
2. **D3:** Route `/admin/orgs/:slug` → `/orgs/:slug` — already done by PR #192 (route is `/orgs/:id`).

---

## Work Completed

### PortalLayout component (D2)

- **`frontend/src/components/layout/PortalLayout.tsx`** (new) — generic portal layout extracted from `OrgAdminLayout`. Props: `title`, `backLink`, `backLabel?`, `badges?`, `meta?`, `navItems`, `context`. Renders the header (back link + title + badges + meta), navigation sidebar (NavLink items), and `<Outlet>` for child routes. Full-screen (no AppShell/Sidebar).
- **`frontend/src/components/layout/PortalLayout.test.tsx`** (new) — 8 tests: title render, back link, nav items, outlet content, custom back label, badges, meta, full-screen class.

### OrgAdminLayout refactor

- **`frontend/src/components/org-admin/OrgAdminLayout.tsx`** — refactored to use `PortalLayout`. OrgAdminLayout now handles only data fetching (orgsApi.get), error/loading states, and org-specific prop computation (badges, meta, filtered nav items). The rendering is delegated to PortalLayout. All 8 existing OrgAdminLayout tests pass unchanged.

### D3 (route rename)
Already done by PR #192 — route is `/orgs/:id`. No changes needed.

---

## Tests Run

- `npx tsc --noEmit` — PASS
- `npx vitest run` — PASS (106 files, 1102 tests)

---

## Files Modified

### New files
- `frontend/src/components/layout/PortalLayout.tsx`
- `frontend/src/components/layout/PortalLayout.test.tsx`

### Modified files
- `frontend/src/components/org-admin/OrgAdminLayout.tsx` — refactored to use PortalLayout

### Branch
`feat/epic43-0031-story6-portal-layout` (from `main`)
