# Worklog: US-35.7 Comprehensive Test Coverage

**Date:** 2026-06-23
**Session:** Wrote 24 tests across 5 files covering every failure mode of US-35.7 (tmpfs credential paths).
**Status:** Complete

---

## Objective

The US-35.7 implementation PR (#384) shipped with insufficient test coverage. The core security property (no plaintext on PVC at rest) was completely untested. This PR adds comprehensive coverage at every level where a failure actually manifests.

---

## Work Completed

### Level 1: Path Resolution Consistency (`path_consistency_test.go`)
4 tests verifying all three path-resolution mechanisms agree on tmpfs paths.

### Level 2: Controller Pod Spec (`security_test.go`, strengthened)
Added init script syntax check (`sh -n`), execution test (runs symlink commands via shell), and init container ordering assertion (credential-setup after workspace-dirs).

### Level 3: Symlink Lifecycle (`symlink_lifecycle_test.go`)
4 tests using real filesystem + t.TempDir to simulate the PVC/tmpfs split. Includes the core security property test (`TestSimulatedPodDeath_NoPlaintextOnPVC`).

### Level 4: Adversarial Edge Cases (`symlink_edge_test.go`)
6 tests covering double-reset, first-boot race, unbind-all, dangling symlinks, direct paths, and full reload cycles.

### Level 4b: Per-Type + Migration (`symlink_type_coverage_test.go`)
4 tests verifying all 5 credential types write to tmpfs, permissions preserved through symlinks, auth.json relay injector path, and pre-US-35.7 directory replacement.

---

## Files Modified

- `cmd/workspace-agentd/path_consistency_test.go` (new)
- `pkg/agentd/secrets/symlink_lifecycle_test.go` (new)
- `pkg/agentd/secrets/symlink_edge_test.go` (new)
- `pkg/agentd/secrets/symlink_type_coverage_test.go` (new)
- `controller/internal/workspace/security_test.go` (strengthened)
