# Worklog: Epic 30 US-30.3 — Auto-apply + Workspace Seeding Hook

**Date:** 2026-06-05
**Session:** Implement auto-apply rules CRUD and workspace credential seeding
**Status:** Complete

---

## Objective

Wire automatic credential provisioning: when a workspace is created, seed workspace_credential_bindings from matching credential_auto_apply rules. Expose admin endpoints to manage auto-apply rules.

---

## Work Completed

### Workspace Seeding
- Added `CredentialProvisioner` interface to `workspace.Service`
- Wired `SeedWorkspaceCredentials(ctx, workspaceID, userID)` call in `CreateWorkspace` at the existing auto-provision comment (line 214)
- Non-fatal: failure logs warning and workspace creation proceeds

### Auto-apply Endpoints
- `POST /api/v1/admin/provider-credentials/:id/auto-apply` — create rule
- `GET /api/v1/admin/provider-credentials/:id/auto-apply` — list rules
- `DELETE /api/v1/admin/provider-credentials/:id/auto-apply/:targetType/:targetId` — delete rule

### DB Implementation
- `CreateAutoApply`, `DeleteAutoApply`, `ListAutoApply` on `PgSecretStore`
- `AutoApplyRule` type in `pkg/secrets`
- Handles NULL target_id correctly for `target_type='all'`

### Wiring
- `pgStore` satisfies both `CredentialProvisioner` and `AutoApplyStore`
- Wired in app.go: `wsSvc.SetCredentialProvisioner(pgStore)` and `adminProvCredHandler.SetAutoApplyStore(pgStore)`

---

## Tests Run

```
go build ./...                    → OK
go test -short -race ./...        → 41 packages OK
make lint                         → 0 issues
```

---

## Next Steps

US-30.4: Free-tier key migration — `EnsureFreeTierCredential` at API startup, remove `OPENCODE_AUTH_CONTENT` from pod_builder.go.

---

## Files Modified

- `api/internal/services/workspace/workspace_service.go` — added `credProvisioner` field, `CredentialProvisioner` interface, seeding call
- `api/internal/handlers/admin_provider_credentials.go` — added `AutoApplyStore` interface, `SetAutoApplyStore`, three endpoint methods
- `api/internal/server/router.go` — added 3 auto-apply routes
- `api/internal/app/app.go` — wired `SetCredentialProvisioner` and `SetAutoApplyStore`
- `pkg/secrets/pg_credential_store.go` — added `AutoApplyRule` type + 3 DB methods
