# Worklog: Epic 30 US-30.6 — User LLM Provider CRUD API

**Date:** 2026-06-05
**Session:** Implement user-scoped provider credentials with user DEK encryption
**Status:** Complete

---

## Objective

Expose user-scoped CRUD for LLM provider credentials encrypted with the user's DEK, plus workspace binding/unbinding endpoints.

---

## Work Completed

- `UserProviderCredentialsHandler`: Create, List, Get, Delete, Bind, Unbind
- `UserCredentialStore` interface
- `UserCredentialRow` type in `pkg/secrets`
- DB methods: CreateUserCredential, ListUserCredentials, GetUserCredential, UpdateUserCredential, DeleteUserCredential, BindCredentialToWorkspace, UnbindCredentialFromWorkspace
- Routes: `/api/v1/provider-credentials` (auth required, no admin guard)
- Bind uses `ON CONFLICT DO UPDATE SET source_type='explicit'` — explicit always overwrites auto

---

## Tests Run

```
go build ./...         → OK
go test -short -race   → 41 packages OK
make lint              → 0 issues
```

---

## Next Steps

US-30.11 (Redis model cache — independent) and US-30.9 (classifyAvailability). Then US-30.12 integration test.

---

## Files Created/Modified

- `api/internal/handlers/user_provider_credentials.go` — new
- `api/internal/server/router.go` — user credential routes
- `api/internal/app/app.go` — wired handler
- `pkg/secrets/pg_credential_store.go` — UserCredentialRow + 7 DB methods
