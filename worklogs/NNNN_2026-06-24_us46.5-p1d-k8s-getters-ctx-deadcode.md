# Worklog: US-46.5 P1-d — K8s getters ctx + dead-code deletion (#224)

**Date:** 2026-06-24
**Session:** Fourth migration from the #224 ctx-propagation audit (P1-d)
**Status:** Complete

---

## Objective

Execute P1-d: propagate ctx through the surviving K8s workspace getter, and
delete the dead-code getters the audit surfaced (`GetWorkspaceCRD`,
`GetWorkspacePassword`).

---

## Work Completed

### ctx propagation

- `k8sWorkspaceGetterAdapter.GetWorkspace(ctx, id)` — uses the passed ctx for
  the K8s `Get` (was `context.Background()`).
- `handlers.WorkspaceGetter` interface + `app.workspaceCRDGetter` interface
  (two separate interfaces the adapter satisfies) — both take ctx.
- Live callers: `handlers/terminal.go` (×2) → `c.Request.Context()`;
  `app/secrets_adapters.go` resolver → its in-scope `ctx`.

### Dead-code deletion (Rule 5)

- `ProxyHandler.GetWorkspaceCRD` — 0 callers. Deleted (also dropped the now-
  unused `v1` import from `proxy_handlers.go`).
- `k8sWorkspaceGetterAdapter.GetWorkspacePassword` — 0 production callers
  (only satisfied the interface). Deleted from the adapter + the
  `WorkspaceGetter` interface + 2 test mocks (`mockWorkspaceGetter`,
  `terminalMockWSGetter`).

### Test mocks updated

`mockWorkspaceGetter`, `terminalMockWSGetter`, `fakeWorkspaceCRDGetter`,
`fakeAppCRDGetter` — all updated to the `GetWorkspace(ctx, id)` signature.

---

## Key Decisions

- **Delete the dead getters rather than migrate them.** Both had 0 callers;
  migrating dead code (adding ctx) would preserve tech debt. Deletion is the
  Rule 5 move the audit endorsed.
- **Two interfaces updated.** The adapter satisfies both `WorkspaceGetter`
  (terminal) and `workspaceCRDGetter` (secrets pod-IP resolver); both needed
  the ctx change to keep the adapter assignable.

---

## Blockers

None.

---

## Tests Run

- `go build` / `go vet` / `make fmt-check` — clean.
- `golangci-lint run --new-from-rev=origin/main` — 0 issues.
- `go test ./api/internal/app/... ./api/internal/handlers/... ./api/internal/server/...`
  — pass.

(No new propagation test added: `GetWorkspace` proxies straight to the K8s
client `Get(ctx, …)` — the K8s client honors ctx by construction, and the
existing terminal/resolver tests exercise the wiring. P1-a/P1-b/P1-c added
propagation tests where the I/O layer was mockable; the K8s client here is the
boundary.)

---

## Next Steps

Last #224 migration: P2 (`RevokeToken`, `RevokeAllUserSessions`).

---

## Files Modified

- `api/internal/app/secrets_adapters.go` — GetWorkspace(ctx); deleted
  GetWorkspacePassword; workspaceCRDGetter interface; resolver caller.
- `api/internal/handlers/proxy_handlers.go` — deleted GetWorkspaceCRD + unused import.
- `api/internal/handlers/terminal.go` — WorkspaceGetter interface; 2 callers.
- `api/internal/handlers/terminal_test.go`, `api/internal/server/router_terminal_test.go`,
  `api/internal/app/secrets_podip_resolver_test.go`, `api/internal/app/secrets_wiring_test.go`
  — mock signature updates + GetWorkspacePassword removal.
- `worklogs/NNNN_2026-06-24_us46.5-p1d-k8s-getters-ctx-deadcode.md` — this file.
