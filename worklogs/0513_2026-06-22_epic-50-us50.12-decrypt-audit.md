# Worklog: Epic 50 — US-50.12 Decrypt Audit Logging

**Date:** 2026-06-22
**Session:** AuditedProvider wrapper — logs every Decrypt to secret_audit_log.
**Status:** Complete.

## Work
- `audited_provider.go` (new): AuditedProvider wraps RootKeyProvider; every Decrypt
  produces one AuditEntry (fire-and-forget goroutine, never blocks). Encrypt is NOT
  logged. Logs: label, success/failure, key_version, timestamp. Never logs
  plaintext/ciphertext/key material.
- `audited_provider_test.go` (new): 5 design-mandated tests (decrypt-logs-entry,
  decrypt-failure-logs-success-false, encrypt-not-logged, no-key-material-in-log,
  async-does-not-block).
- AuditWriter interface + ContextWithDecryptUser for user attribution.

## Tests
build OK; pkg/secrets -race green (48s); golangci-lint 0 issues.
