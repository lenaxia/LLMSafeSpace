{{- if .Values.dbInit.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "llmsafespaces.fullname" . }}-db-init
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llmsafespaces.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-init
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.dbInit.backoffLimit }}
  ttlSecondsAfterFinished: {{ .Values.dbInit.ttlSecondsAfterFinished }}
  template:
    metadata:
      labels:
        {{- include "llmsafespaces.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: db-init
    spec:
      restartPolicy: OnFailure
      {{- with .Values.api.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: db-init
          image: "{{ .Values.dbInit.image.repository | default "postgres" }}:{{ .Values.dbInit.image.tag | default "16-alpine" }}"
          imagePullPolicy: {{ .Values.dbInit.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop:
                - ALL
          env:
            - name: PGHOST
              value: {{ .Values.postgresql.host | quote }}
            - name: PGPORT
              value: {{ .Values.postgresql.port | quote }}
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: {{ required "dbInit.superuserSecret.name is required when dbInit.enabled=true" .Values.dbInit.superuserSecret.name | quote }}
                  key: {{ .Values.dbInit.superuserSecret.userKey | default "username" | quote }}
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ required "dbInit.superuserSecret.name is required when dbInit.enabled=true" .Values.dbInit.superuserSecret.name | quote }}
                  key: {{ .Values.dbInit.superuserSecret.passwordKey | default "password" | quote }}
            - name: APP_USER
              value: {{ .Values.postgresql.user | quote }}
            - name: APP_DB
              value: {{ .Values.postgresql.database | quote }}
            - name: APP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "llmsafespaces.secretName" . }}
                  key: postgres-password
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              if psql -d postgres -tAc "SELECT 1 FROM pg_roles WHERE rolname='$APP_USER'" | grep -q 1; then
                psql -d postgres -c "ALTER ROLE \"$APP_USER\" WITH PASSWORD '$APP_PASSWORD'"
              else
                psql -d postgres -c "CREATE ROLE \"$APP_USER\" LOGIN PASSWORD '$APP_PASSWORD'"
              fi
              if psql -d postgres -tAc "SELECT 1 FROM pg_database WHERE datname='$APP_DB'" | grep -q 1; then
                psql -d postgres -c "ALTER DATABASE \"$APP_DB\" OWNER TO \"$APP_USER\""
              else
                psql -d postgres -c "CREATE DATABASE \"$APP_DB\" OWNER \"$APP_USER\""
              fi
          resources:
            {{- toYaml .Values.dbInit.resources | nindent 12 }}
{{- end }}
