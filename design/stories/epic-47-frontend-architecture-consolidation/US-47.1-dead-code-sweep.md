# US-47.1 — Dead-code sweep — remove all orphaned frontend files

| Field | Value |
|-------|-------|
| Epic | 47 — Frontend Architecture Consolidation |
| Priority | High (Phase 1 prerequisite) |
| Depends On | None |
| Blocks | None (but shrinks every later diff) |
| Estimated | 1–2 hours |
| Evidence | `design/0037` F4/F5, `design/0039` §4 |

## Summary

Remove dead frontend files that were orphaned by architectural evolution but never cleaned up. Each file has zero production importers (validated by grep). Every file has co-located tests that must also be deleted.

## Files to delete

### HTTP-streaming parser (orphaned by SSE move)
- `frontend/src/lib/stream.ts` — `extractStreamText` / `ParsedStreamResult`. Zero production importers. ChatPage's `parseStreamEvent` is a different function (parses SSE events, not HTTP stream chunks).
- `frontend/src/lib/stream.test.ts`
- `frontend/src/lib/stream.edge.test.ts`

### Admin credential tab (backend table dropped)
- `frontend/src/components/settings/AdminCredentialsTab.tsx` — targets `credential_sets` table, dropped in migration `000015:10`. Backend route `/admin/credentials` not registered in `router.go`. Never rendered in `SettingsPage.tsx`.
- `frontend/src/components/settings/AdminCredentialsTab.test.tsx`

### BroadcastChannel SSE client (never wired)
- `frontend/src/api/events.ts` — `createEventStream` with leader election. Only importers are test files (`api/events.test.ts`, `useChatStream.test.ts:13`). Live streams use `lib/sseConnection.ts`.
- `frontend/src/api/events.test.ts`

### Parallel session-list cluster (superseded by Sidebar tree)
- `frontend/src/components/session/SessionItem.tsx` — only imported by `SessionList.tsx` + test.
- `frontend/src/components/session/SessionItem.test.tsx`
- `frontend/src/components/session/SessionList.tsx` — only imported by dead `WorkspaceSessionList.tsx` + test.
- `frontend/src/components/session/SessionList.test.tsx`
- `frontend/src/components/workspace/WorkspaceSessionList.tsx` — zero production importers.

### Dead API methods and duplicate types
- `frontend/src/api/workspaces.ts:55` — `getActiveSessions` (zero callers)
- `frontend/src/api/workspaces.ts:56` — `getWorkspaceSessions` (zero callers, also wrong-typed: `WorkspaceListItem[]` for a sessions endpoint)
- `frontend/src/api/workspaces.ts:49-50` — `ensureSession` + `EnsureSessionResponse` (duplicate of `sessions.ts:create` + `CreateSessionResponse`, same endpoint)

### Dead hooks
- `frontend/src/hooks/useSessions.ts` — only reference is a comment in `ChatPage.tsx:127`
- `frontend/src/hooks/useSessions.test.tsx`
- `frontend/src/hooks/useWorkspaces.ts` — the `useWorkspaces` (list) export is dead; `useWorkspaceStatus` from the same file IS used and must be preserved. **Do not delete the file** — remove only the dead `useWorkspaces` function and update the test.

## Acceptance Criteria

1. All listed files/functions removed.
2. `useWorkspaces.ts` retains `useWorkspaceStatus` (used by `ChatPage.tsx:6`).
3. `npm run typecheck` passes (no broken imports).
4. `npm run test` passes (no test references deleted symbols).
5. `npm run lint` passes.
6. `npm run build` passes.
7. Grep confirms zero remaining references to deleted symbols in non-test production code.
