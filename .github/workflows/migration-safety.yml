name: Migration safety

# Validates database migration safety:
#
#   1. Round-trip: apply all up migrations, snapshot schema, apply all
#      down migrations in reverse, re-apply ups, snapshot again. The two
#      schema snapshots must match (proves down migrations actually
#      reverse their corresponding up). This catches the class of
#      migration drift that bit us live during pass-6 live validation
#      (worklog 0099 / migration 000009 image_tag drift).
#
#   2. FK cascade integrity: insert a sample row in every parent table,
#      delete the parent, assert no orphan rows in any child. Catches
#      regressions like "binding rows survive a workspace delete"
#      (worklog 0094 pass-2 audit hazard O11).
#
#   3. Idempotency: apply ups twice. Migrations using IF NOT EXISTS /
#      IF EXISTS clauses should succeed on both runs. Catches the class
#      where someone uses CREATE TABLE without IF NOT EXISTS, breaking
#      the redeploy path.
#
#   4. Data cleanup (migration 000014 regression): apply migrations 1-13,
#      seed edge-case rows (non-UUID workspace_id bindings, orphan
#      workspaces), apply 000014, and assert the cleanup logic ran
#      correctly. Self-contained regression for the hardened data-migration
#      behaviour introduced in worklog 6c8b0a72; pinned to migration 14 by
#      design (it is a point-in-time regression, not a forward-going check).
#      Kept in parity with the local `make migration-safety` composite target
#      so local and CI run the identical set of four checks.
#
# Why a separate workflow: same isolation argument as security-scan.yml
# — failure here is a database-shape regression, not a "tests broke" one,
# and we want the alert to be unambiguous. The scripts that drive each
# check live in hack/ and api/migrations/test/ so developers can run
# them locally with `make migration-safety` against a docker postgres.

on:
  push:
    branches: [main]
    paths:
      - 'api/migrations/**'
      - 'hack/migration-*.sh'
      - '.github/workflows/migration-safety.yml'
  pull_request:
    branches: [main]
    paths:
      - 'api/migrations/**'
      - 'hack/migration-*.sh'
      - '.github/workflows/migration-safety.yml'
  workflow_dispatch:

permissions:
  contents: read

# Common Postgres service container reused by every job. Each job
# starts with a fresh database, so the jobs are independent and can
# run in parallel.
jobs:
  round-trip:
    name: Migration up→down→up round-trip
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: llmsafespaces
          POSTGRES_PASSWORD: migration-test
          POSTGRES_DB: llmsafespaces
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llmsafespaces"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      PGHOST: localhost
      PGUSER: llmsafespaces
      PGPASSWORD: migration-test
      PGDATABASE: llmsafespaces

    steps:
      - uses: actions/checkout@v6
      - name: Wait for Postgres
        run: |
          for i in {1..20}; do
            pg_isready -h localhost -U llmsafespaces && break
            sleep 1
          done
      - name: Round-trip
        run: bash hack/migration-roundtrip.sh

  idempotent:
    name: Migration idempotency (apply ups twice)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: llmsafespaces
          POSTGRES_PASSWORD: migration-test
          POSTGRES_DB: llmsafespaces
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llmsafespaces"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      PGHOST: localhost
      PGUSER: llmsafespaces
      PGPASSWORD: migration-test
      PGDATABASE: llmsafespaces

    steps:
      - uses: actions/checkout@v6
      - name: Wait for Postgres
        run: |
          for i in {1..20}; do
            pg_isready -h localhost -U llmsafespaces && break
            sleep 1
          done
      - name: Idempotency
        run: bash hack/migration-idempotent.sh

  fk-cascade:
    name: FK cascade integrity
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: llmsafespaces
          POSTGRES_PASSWORD: migration-test
          POSTGRES_DB: llmsafespaces
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llmsafespaces"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      PGHOST: localhost
      PGUSER: llmsafespaces
      PGPASSWORD: migration-test
      PGDATABASE: llmsafespaces

    steps:
      - uses: actions/checkout@v6
      - name: Wait for Postgres
        run: |
          for i in {1..20}; do
            pg_isready -h localhost -U llmsafespaces && break
            sleep 1
          done

      # FK cascade test needs the schema applied first; the script
      # itself doesn't bootstrap (round-trip and idempotent jobs reset
      # the schema in their first step, so we follow the same pattern
      # here for symmetry).
      - name: Apply all up migrations
        run: |
          set -euo pipefail
          for f in $(ls api/migrations/*.up.sql | sort); do
            psql -v ON_ERROR_STOP=1 -f "$f"
          done

      - name: FK cascade
        run: bash api/migrations/test/fk_cascade.sh

  data-cleanup:
    name: Migration 000014 data-cleanup regression
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: llmsafespaces
          POSTGRES_PASSWORD: migration-test
          POSTGRES_DB: llmsafespaces
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llmsafespaces"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      PGHOST: localhost
      PGUSER: llmsafespaces
      PGPASSWORD: migration-test
      PGDATABASE: llmsafespaces

    steps:
      - uses: actions/checkout@v6
      - name: Wait for Postgres
        run: |
          for i in {1..20}; do
            pg_isready -h localhost -U llmsafespaces && break
            sleep 1
          done

      # The data-cleanup script is self-contained: it resets the public
      # schema, applies migrations 000001-000013, seeds edge-case rows,
      # applies 000014, and asserts the cleanup ran. No pre-apply step
      # needed (unlike fk-cascade which expects the caller to apply all
      # ups first).
      - name: Data cleanup (migration 000014)
        run: bash hack/migration-data-cleanup.sh
